ARG BUILD_FROM
# The slicer image is self-contained (Ubuntu 24.04 base, glibc >= 2.38, bundles
# OrcaSlicer). BUILD_FROM (the HA base image) is intentionally ignored.
FROM ghcr.io/schmacka/printernizer-slicer:latest
